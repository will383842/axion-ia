# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Sprint S6.3 P0-1 (2026-05-15) — bloque création accidentelle d'un
# package-lock.json (npm) qui doublonnerait pnpm-lock.yaml.
package-lock.json

# testing
/coverage
/playwright-report
/test-results
/lhci

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# env files (only *.example variants are committed)
.env
.env.*
!.env.example
!.env.dev.example
!.env.production.example
!.env.ci.example

# vercel (we don't deploy to Vercel but keep ignore)
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma generated
/prisma/generated

# OS
Thumbs.db

# Sentry
.sentryclirc
.next/build-manifest.json

# Runtime data (RUM Web Vitals ndjson rotatif — P-303)
/data/
