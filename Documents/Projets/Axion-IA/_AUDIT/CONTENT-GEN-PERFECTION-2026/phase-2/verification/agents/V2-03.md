# V2-03 — P0-3 promptHash réel SHA256 (AI Act art. 50)
**Date** : 2026-05-21 | **Score : 0 / 100** | **AI Act : 🔴 NON-CONFORME**

---

## Verdict : NON-CORRIGÉ — CRITIQUE AI Act

### P0-3 absent du commit 17c53bc

Le commit principal du sprint liste explicitement les P0 corrigés :
> P0-1, P0-4, P0-5, P0-6, P0-7, P0-8, P0-9, P0-10

**P0-3 est absent de la liste.** Il n'a pas été corrigé.

---

## Constat code

`content-publish-worker.ts` ligne 370 :
```typescript
// Prompt hash derive du contentType + jobId (pas le prompt complet — PII).
const promptHash = hashPrompt(`${cgJob.contentType}:${cgJob.id}:${article.id}`);
```

**Ce qui est hashé** : `SHA256("blog_article:<uuid-job>:<uuid-article>")` — des identifiants techniques opaques.

**Ce qui DEVRAIT être hashé** : le prompt LLM réel soumis à Claude (systemPrompt + userPrompt).

---

## Analyse de la chaîne prompt

| Maillon | État | Problème |
|---------|------|----------|
| Generators (`blog-article.ts`, etc.) | `userPrompt` + `systemPrompt` construits localement | Ne retournent pas le prompt dans `GeneratorOutput` |
| `GeneratorOutput` type | Aucun champ `promptUsed`/`systemPrompt` | ❌ Prompt non transmissible |
| `content-gen-worker.ts` | Ne persiste pas le prompt dans `outputJsonRaw` | ❌ Prompt perdu après génération |
| `content-publish-worker.ts` ligne 370 | Hash un triplet d'IDs | ❌ Sémantiquement creux |

---

## Scoring

| Critère | Max | Score |
|---------|-----|-------|
| promptHash = SHA256(prompt LLM réel) | 60 | 0 |
| Prompt transmis gen→publish via job.data | 20 | 0 |
| Test unitaire valide le promptHash réel | 20 | 5 (test mécanique, pas sémantique) |
| MALUS SHA256(contentType:jobId:articleId) | -100 | -100 |
| **TOTAL** | **100** | **0** |

---

## Compliance AI Act art. 50 : 🔴 NON-CONFORME

La deadline AI Act art. 50 est **2026-08-02 (< 3 mois)**. Le système ne peut pas prétendre à la conformité avec un `promptHash` qui ne hash pas le prompt LLM réel — une autorité de contrôle ne peut pas reconstituer ni vérifier le prompt original utilisé pour générer un article.

## Effort correctif estimé : 2-3h
1. Ajouter `promptUsed?: string` dans `GeneratorOutput` (types.ts)
2. Les 7 generators retournent le prompt dans `GeneratorOutput`
3. `content-gen-worker.ts` persiste `promptUsed` dans `outputJsonRaw` ou `job.data`
4. `content-publish-worker.ts` ligne 370 : `hashPrompt(cgJob.outputJsonRaw?.promptUsed ?? '')`
5. Ajouter 1 test sémantique : SHA256(prompt) correspond à `GenerationProvenance.promptHash`
