# V2-01 — P0-1 CASCADE DELETE → RESTRICT (AI Act art. 50)
**Date** : 2026-05-21 | **Score : 65 / 100** | **AI Act : 🟡 GAPS**

---

## Verdict par critère

| Critère | Max | Score | Statut |
|---------|-----|-------|--------|
| schema.prisma `onDelete: Restrict` | 50 | 0 | ❌ Encore `onDelete: Cascade` ligne 982 |
| Migration SQL RESTRICT dans commit 17c53bc | 25 | 25 | ✅ Migration `20260521150000_fix_provenance_cascade` |
| Mécanisme RGPD art.17 (soft-delete ou deleteArticleAndProvenance) | 15 | 15 | ✅ Endpoint `/api/admin/articles/[id]/forget` — MAIS bug P0 |
| Pas de régression provenance-logger | 10 | 10 | ✅ 6 tests unitaires, création correcte |
| **TOTAL** | **100** | **65** | 🟡 |

---

## Désynchronisation forensique critique

| Artefact | Valeur | Statut |
|----------|--------|--------|
| Migration `20260521120100` (création table) | `ON DELETE CASCADE` | Ancienne, écrasée |
| Migration `20260521150000` (fix P0-1) | `ON DELETE RESTRICT` | ✅ Appliquée si `prisma migrate deploy` exécuté |
| `prisma/schema.prisma` ligne 982 | `onDelete: Cascade` | ❌ NON mis à jour = **drift** |
| `forget/route.ts` commentaire | "CASCADE from schema" | ❌ Incohérent avec RESTRICT |

**Conséquence** : la prochaine migration régénérée par Prisma réintroduira `ON DELETE CASCADE` en DB, annulant le fix RESTRICT. Le drift est un risque systémique.

---

## Bug P0 résiduel critique

`forget/route.ts` appelle `prisma.article.delete({ where: { id } })` sans supprimer au préalable les `GenerationProvenance`. Ce code dépend du CASCADE. Si la migration RESTRICT est active en DB :
- **Appel DELETE article → FK violation** → droit à l'oubli RGPD art. 17 **inapplicable en prod**
- Conflit AI Act art. 50 (traçabilité) × RGPD art. 17 (effacement) non résolu

---

## Test fonctionnel
DB inaccessible depuis cet environnement d'audit. Déduction : si `prisma migrate deploy` exécuté, RESTRICT actif. Test psql : `\d generation_provenance` → contrainte FK à vérifier manuellement.

## Compliance AI Act art. 50 : 🟡 GAPS
- Migration RESTRICT présente ✅
- schema.prisma non synchronisé → risque de régression future ❌
- forget/route.ts cassé avec RESTRICT → conflit RGPD/AI Act non résolu ❌

## Risques résiduels
- **P0** : `forget/route.ts` FK violation certaine en prod avec RESTRICT actif
- **P0** : schema.prisma drift → future migration annule le fix
- **P1** : Aucun test d'intégration couvrant RESTRICT + oubli RGPD
