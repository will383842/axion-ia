# V2-07 — P0-7 Rate limit workers image-bank
**Date** : 2026-05-21 | **Score : 40 / 40** | **Statut : ✅ GO**

---

## Verdict par critère

| Critère | Max | Score | Statut |
|---------|-----|-------|--------|
| `image-bank-enrich-worker.ts` a `limiter: {max:10, duration:60_000}` | 15 | 15 | ✅ Présent (commit 17c53bc) |
| `image-bank-translate-worker.ts` a `limiter: {max:10, duration:60_000}` | 15 | 15 | ✅ Présent (commit 17c53bc) |
| Durée correcte en ms (60_000 et non 60) | 10 | 10 | ✅ `60_000` = séparateur numérique TS = 60000 ms |
| **TOTAL** | **40** | **40** | ✅ |

---

## Détail

### image-bank-enrich-worker.ts
```typescript
limiter: { max: 10, duration: 60_000 },
// P0-7 — Rate limit Claude : 10 jobs/min max (aligné quota Claude API).
```

### image-bank-translate-worker.ts
Bloc identique.

### Autres workers image-bank (non concernés)
- `image-bank-auto-convert-worker.ts` : Sharp uniquement (CPU-bound), pas d'appel Claude — absence justifiée
- `image-bank-import-worker.ts` : Sharp + Prisma, enqueue des jobs enrich mais pas d'appel IA direct
- `image-bank-crons-worker.ts` : orchestrateur cron, pas d'appel API externe

Les deux seuls workers appelant l'API Anthropic ont reçu le `limiter`. Correction P0-7 complète et correctement ciblée.

### Note technique
`60_000` en TypeScript = séparateur numérique (underscore) = `60000` ms = 60 secondes. BullMQ v5 attend `duration` en millisecondes. La valeur est correcte.
