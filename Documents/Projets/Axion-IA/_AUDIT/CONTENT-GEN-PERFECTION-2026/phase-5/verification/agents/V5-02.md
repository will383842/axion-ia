# V5-02 — P0-1 Boutons pause/resume liste
**Score : 80/80** ✅

## CoverageListV2 boutons pause/resume
- ✅ Présents aux lignes 175-214 de `src/components/admin/content-gen/coverage/_v2/CoverageListV2.tsx`
- `status === "running"` → icône `<Pause />` lucide-react
- `status === "paused"` → icône `<Play />` lucide-react
- `status === "draft"` → icône `<PlayCircle />` lucide-react

## Logic status running/paused
- ✅ Condition ternaire propre : `r.status === "running" ? pauseRow : r.status === "paused" ? resumeRow : ...`
- Non visible pour `completed`/`failed` (bouton rendu seulement pour running/paused/draft)

## Server Actions pause/resume
- ✅ `pauseCampaign(id)` dans `coverage.ts` lignes 219-273
  - Bascule `status → "paused"`
  - Purge jobs BullMQ `waiting/delayed` (best-effort)
  - Log activité
- ✅ `resumeCampaign(id)` dans `coverage.ts` lignes 275-329
  - Bascule `status → "running"`
  - Archive `pausedAt` via `writeContentGenConfig`
  - Log activité

## Tooltip + style neutral
- ✅ `title="Mettre en pause"` et `title="Reprendre"` (tooltip natif HTML)
- ✅ Classe `admin-button-ghost` = neutre (pas terracotta)
- ✅ Icon-only design

## TOTAL V5-02 : 80/80

## Items manquants 🔴
Aucun

## Tests UI (code-only)
Code 100% conforme. Test navigateur réel requis pour confirmer BullMQ purge au pause.
