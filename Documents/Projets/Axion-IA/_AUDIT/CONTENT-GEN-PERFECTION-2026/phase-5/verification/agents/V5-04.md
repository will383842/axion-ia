# V5-04 — P0-3 MAX_PUBLISH_PER_DAY UI
**Score : 40/40** ✅

## Worker lit DB
- ✅ `content-publish-worker.ts` ligne 86-97 : `getEffectivePublishCap()`
  - Priorité 1 : env var `MAX_PUBLISH_PER_DAY`
  - Priorité 2 : DB `readContentGenConfig<number>("MAX_PUBLISH_PER_DAY", 0)` (ligne 90)
  - Priorité 3 : rampe progressive basée sur article count
  - Fix corrigé dans commit `e573da6` ✅

## Server actions get/update
- ✅ `getMaxPublishPerDay()` → `readContentGenConfig<number>("MAX_PUBLISH_PER_DAY", 30)` dans `policies.ts:85-87`
- ✅ `updateMaxPublishPerDay(value)` → validation 1-1000 + `writeContentGenConfig` dans `policies.ts:89-101`

## UI input avec range
- ✅ `BatchesV2.tsx` lignes 57-83 : input `type="number" min="1" max="1000" step="10"`
- ✅ Label "Cap articles/jour"
- ✅ Form action → `saveMaxPublish` → `updateMaxPublishPerDay()`

## Audit trail SOC2
- ✅ `writeContentGenConfig()` dans `_settings.ts` lignes 42-91 :
  - Lit valeur existante avant remplacement
  - Appelle `writeAuditLog` avec diff `oldValue → newValue`
  - Rate-limit 60/min via `requireAdminWriteRateLimited`

## TOTAL V5-04 : 40/40
