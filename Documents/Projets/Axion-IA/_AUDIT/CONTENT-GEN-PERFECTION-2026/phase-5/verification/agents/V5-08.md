# V5-08 — ArticleFeedback + tableau croisé + progress bars + dashboard actif + anomaly
**Score : 163/180** ✅ (MÀJ HEAD 240f8b8b)

## ArticleFeedback (/40 → 40/40) ✅ RÉSOLU
- ✅ Modèle `ArticleFeedback` dans schema.prisma
- ✅ Route `POST /api/admin/content-gen/articles/[id]/feedback/route.ts`
- ✅ **UI thumbs up/down dans ReviewDetailV2** (commit 95805342) :
  ```jsx
  {articleId && (
    <AdminCard>
      <h2>Feedback éditorial</h2>
      <form action={submitFeedback}><input name="type" value="up"/><button>👍 Bon</button></form>
      <form action={submitFeedback}><input name="type" value="down"/><button>👎 À améliorer</button></form>
    </AdminCard>
  )}
  ```
- ✅ Server action `submitFeedback` → `prisma.articleFeedback.create()`

**Score : 40/40** ✅

## Tableau croisé géo (/50 → 43/50)
- ✅ `getJobsVilleSectorDetail()` dans `geo.ts` — groupBy + batch-join campaigns pour serviceSector
- ✅ `CoverageCrossTableV2.tsx` : 4+ colonnes dont Ville, État, Secteur, Count, Score moy.
- ✅ Filtres URL : `ville` + `status` query params (commit 95805342)
- ✅ Pagination 50/page : `PAGE_SIZE = 50` + boutons nav
- ✅ Export CSV : route `/coverage-table/export.csv/route.ts` avec auth admin + headers corrects
- ⚠️ Tri colonne serveur : filtres URL présents, sort param absent → -7

**Score : 43/50**

## Progress bars (/40 → 35/40)
- ✅ **Barre X/120 villes dans dashboard** (commit 240f8b8b) :
  - `getCityCoverageProgress()` dans `geo.ts` → `{ publishedVilles, targetVilles: 120, pct }`
  - Barre `<progress>` HTML avec `accentColor` conditionnel (33%/66%)
- ✅ **OrchestratorV2 : progress bar par campagne** (commit 240f8b8b)
- ⚠️ Seuils couleur : pct < 33 rouge, pct < 66 orange, sinon vert (spec demandait 33%/66% → ✅)
- ⚠️ CoverageDetailV2 : toujours texte `{X}/{Y}` sans barre HTML → -5

**Score : 35/40**

## Dashboard campagnes actives (/30 → 27/30)
- ✅ `OrchestratorV2.tsx` : table campagnes actives
- ✅ **ETA calculé** (commit 240f8b8b) : `etaDays = Math.ceil(remaining / (velocity7d / 7))`
- ✅ Colonne ETA `~Xj` dans table
- ✅ Progress bar avancement par campagne (pct généré)
- ⚠️ "Articles générés aujourd'hui" par campagne : non présent → -3

**Score : 27/30**

## Anomaly detection batch (/20 → 18/20)
- ✅ `checkAnomalies()` dans `content-monitoring-worker.ts` : 3 checks (qualité, reject, stall)
- ✅ **Badge alertes dans layout content-gen** (commit 240f8b8b) :
  ```jsx
  {alertCount > 0 && (
    <span style={{ backgroundColor: "var(--color-admin-destructive)", color: "white" }}>
      {alertCount} alerte{alertCount > 1 ? "s" : ""}
    </span>
  )}
  ```
- ✅ Bandeau détail anomalies (lien vers /monitoring)
- ⚠️ Spec demandait "badge rouge sidebar" — implémenté dans layout (header), pas sidebar → -2

**Score : 18/20**

## TOTAL V5-08 : 163/180
(précédent : 90/180 — thumbs UI manquant, tableau partiel, progress absentes, ETA absent, badge absent)
