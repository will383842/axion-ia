/**
 * Qualiopi — Types CRM clients + devis (ré-export Prisma, pas de dup).
 */

export type {
  Client,
  Devis,
  ClientStatut,
  DevisStatut,
  CompanySize,
} from "../../../../prisma/generated/client";
