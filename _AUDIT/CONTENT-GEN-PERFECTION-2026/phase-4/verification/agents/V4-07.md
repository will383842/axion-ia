# V4-07 — P0-7 REJECT-P0 vs REJECT-qualité

**Score : 52/60 ✅**
**Date : 2026-05-21 | HEAD : b523f5a4 | Mode : AUDIT-ONLY**

---

## ✅ Implémentation vérifiée

### Distinction REJECT-P0 vs cap-reached

`content-quality-improver-worker.ts` :

```typescript
const isHardReject = verdict === "reject";

// P0-7 — nextStatus
const nextStatus = isHardReject
  ? "quarantined_critical" // ← P0 violation
  : shouldRegenerate
    ? "quality_improving"
    : "needs_review"; // ← cap atteint (qualité)
```

✅ REJECT-P0 (LLM-judge verdict="reject" = P0 issue) → `quarantined_critical`
✅ Cap atteint sans P0 → `needs_review`

### Log step distinct

`generation-log.ts` ligne 67 : `"quality_loop_hard_reject"` dans `GenerationLogStep` ✅

`content-quality-improver-worker.ts` :

```typescript
if (isHardReject && judge) {
  const p0Issues = judge.issues.filter((i) => i.severity === "P0");
  await logGeneration({
    jobId: contentGenJobId,
    level: "error",
    step: "quality_loop_hard_reject",
    message: `LLM-judge HARD REJECT — ${p0Issues.length} P0 issue(s) critiques.`,
  });
}
```

✅ Log séparé avec `level: "error"`

### quarantined_critical dans enum ✅

`prisma/schema.prisma` enum `ContentGenJobStatus` contient `quarantined_critical` ✅

---

## ⚠️ Manquant

### Alerte Telegram/email Will

Spec : "alerte immédiate Telegram + email Will" lors d'un HARD REJECT P0.

Réalité : uniquement `logGeneration()` (visible dans GenerationLog table + console). **Pas de webhook Telegram ni email configuré.**

Impact : escalade Will dépend de la surveillance des logs — pas automatique.
Action S+7 : ajouter `TELEGRAM_WEBHOOK_URL` env var + call HTTP dans le bloc `if (isHardReject)`.

---

## Score : 52/60

| Critère                             | Score | Statut |
| ----------------------------------- | ----- | ------ |
| isHardReject → quarantined_critical | 20    | ✅     |
| Cap → needs_review                  | 15    | ✅     |
| Log step quality_loop_hard_reject   | 10    | ✅     |
| GenerationLogStep type étendu       | 7     | ✅     |
| Alerte Telegram/email Will          | 0     | ⚠️     |
