# V4-06 — P0-6 Quarantaine fact-check + FactCheckClaim

**Score : 45/80 ⚠️ PARTIEL**
**Date : 2026-05-21 | HEAD : b523f5a4 | Mode : AUDIT-ONLY**

---

## ✅ Ce qui est livré

### 1. Schema Prisma FactCheckClaim ✅

`prisma/schema.prisma` :
- Modèle `FactCheckClaim` avec tous les champs (id, articleId, claim, status, sourceUrl, sourceTitle, confidence, createdAt) ✅
- `@@map("factcheck_claims")` ✅
- Indexes : `[articleId]` + `[status]` ✅
- FK → `Article` avec `onDelete: Cascade` ✅
- `Article.factCheckClaims FactCheckClaim[]` relation ✅

### 2. Enum ContentGenJobStatus ✅

- `quarantined_critical` ✅ (P0-7)
- `quarantined_factcheck` ✅ (P0-6)

### 3. Migration SQL ✅

`prisma/migrations/20260521160000_add_content_gen_factcheck_claims_quarantine/migration.sql` :
- `ALTER TYPE "ContentGenJobStatus" ADD VALUE IF NOT EXISTS 'quarantined_factcheck'` ✅
- `ALTER TYPE "ContentGenJobStatus" ADD VALUE IF NOT EXISTS 'quarantined_critical'` ✅
- `CREATE TABLE "factcheck_claims" (...)` ✅

### 4. Claims extractor ✅

`src/server/content-gen/fact-check/claims-extractor.ts` :
- `extractClaims(body: string): ExtractedClaim[]` ✅
- `computeFactCheckScore(verdicts[])` ✅

### 5. Fact-check worker ✅

`src/server/queue/workers/content-fact-check-worker.ts` :
- Worker existe, enqueué par `content-publish-worker` ✅
- Appelle Perplexity Sonar pour verification ✅

---

## ❌ Ce qui manque

### Gate dur factCheckScore < 50 → quarantained_factcheck ABSENT ❌

Le worker `content-fact-check-worker.ts` met à jour `Article.factCheckScore` mais **ne modifie pas `ContentGenJob.status`** vers `quarantined_factcheck` si score < 50.

La logique attendue :
```typescript
if (score < FACT_CHECK_QUARANTINE_THRESHOLD) {
  await prisma.contentGenJob.update({
    where: { id: contentGenJobId },
    data: { status: "quarantined_factcheck" }
  });
}
```
**Actuellement absent** — articles avec faible factCheckScore ne sont pas mis en quarantaine.

### Claims individuels non persistés en DB ⚠️

Les claims extraits par `extractClaims()` sont utilisés pour calculer le score, mais ne sont **pas insérés dans la table `factcheck_claims`**. La table existe mais reste vide.

### RAG Voyage AI : stub documenté ✅

Sprint verdict note explicitement "claim verification en mode stub V1 (API différée Sprint S+7)". Acceptable.

---

## Score : 45/80

| Critère | Score | Statut |
|---------|-------|--------|
| Schema FactCheckClaim | 15 | ✅ |
| Migration SQL | 15 | ✅ |
| Enum quarantined_factcheck | 5 | ✅ |
| Claims extractor | 5 | ✅ |
| Fact-check worker | 5 | ✅ |
| Gate dur < 50 → quarantaine | 0 | ❌ ABSENT |
| Claims persistés en DB | 0 | ❌ ABSENT |

**Action requise (P0) :** Ajouter gate `factCheckScore < 50 → status = quarantined_factcheck` dans `content-fact-check-worker.ts` + upsert claims en DB.
