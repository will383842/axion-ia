# V4-02 — P0-2 Boucle improve avec issues[]

**Score : 80/80 ✅**
**Date : 2026-05-21 | HEAD : b523f5a4 | Mode : AUDIT-ONLY**
**Commit pré-sprint :** `0947d9e` "quality loop re-génère avec feedback LLM-judge (BUG 4)"

---

## Vérifications

### 1. issues[] reçues et formatées ✅

`formatJudgeFeedback()` (content-quality-improver-worker.ts l.38-57) :

- Filtre P0 issues (severity + section + suggestedFix) ✅
- Filtre P1 issues ✅
- Dimensions faibles (score < 7/10) ✅
- Score global format : "Score global : X.X/10" ✅

### 2. Persistance judgeIssues dans outputJsonRaw ✅

```typescript
const judgeFeedback = judge ? formatJudgeFeedback(judge) : null;
const updatedOutput = judgeFeedback
  ? { ...existingOutput, judgeIssues: judgeFeedback }
  : existingOutput;
```

### 3. Re-enqueue content-gen avec feedback ✅

`content-quality-improver-worker.ts` l.302-327 : re-enqueue si `shouldRegenerate = true`.

### 4. GeneratorBaseInput.improvementFeedback? ✅

`types.ts` ligne 38 : `readonly improvementFeedback?: string` documenté.

### 5. Content-gen-worker injecte dans generators ✅

`content-gen-worker.ts` lit `prevOutput["judgeIssues"]` → `improvementFeedback` → passé au generator.

### 6. Injection dans prompts ✅

4/4 generators (blog-article, blog-from-keywords, guide-pilier, landing-ville) :

```typescript
const feedbackSection = prevFeedback
  ? `\n\n## Retour qualité passe précédente\n${prevFeedback}\nCorrige impérativement ces points.`
  : "";
```

---

## Score : 80/80

Pipeline complet : judge issues[] → formatJudgeFeedback → outputJsonRaw.judgeIssues → re-enqueue → improvementFeedback → injection prompt.
