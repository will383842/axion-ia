# V5-07 — P1 CampaignTemplate : Presets, Wizard, Prefill
## Score : 55/120

### P1-1 Modèle Prisma CampaignTemplate (/40 → 28/40)
- ✅ schema.prisma : `model CampaignTemplate { id/slug/name/description/config/isSystem/isActive/createdAt/updatedAt }`
- ✅ `@@map("campaign_templates")`, index slug + isActive
- ✅ Migration `20260521170000_add_campaign_template_and_feedback` SQL correcte
- ✅ Seed `seed-campaign-templates.ts` avec 6 upserts idempotents
- ⚠️ Config JSON presets ne contient pas `dailyCap` pour tous les presets (pme-audits OK, tpe-burst OK, interventions-weekly NON)
- ⚠️ Migration non appliquée en prod (-8 pts)
- ⚠️ Pas de script `content-gen:seed-templates` dans package.json final (script pointe vers `seed-campaign-templates-standalone.ts`) — OK fonctionnellement

### P1-2 UI Presets `/coverage/presets` (/40 → 22/40)
- ✅ Page `coverage/presets/page.tsx` + composant `CampaignPresetsV2.tsx`
- ✅ 6 cartes avec nom + description + bouton "Utiliser ce preset"
- ✅ Fallback statique si DB vide (robustesse)
- ✅ Lien vers `/coverage/new?preset=<slug>`
- ❌ Pas à `/content-gen/templates/` (spec) mais à `/coverage/presets/` (route différente)
- ❌ Pas de résumé config par card (verticales, types, batchSize, dailyCap) — spec demandait un aperçu
- ❌ Lien "Nouvelle campagne libre" utilise `admin-button` (gris) pas `admin-button-cta` (terracotta)

### P1-3 Wizard prefill depuis preset (/40 → 5/40)
- ✅ `Props.searchParams.preset?: string` ajouté
- ✅ `prisma.campaignTemplate.findUnique({ where: { slug: sp.preset } })` (via any cast)
- ✅ `presetData` chargé + banner "Preset actif : <name>"
- ❌ Les champs du formulaire NE SONT PAS pré-remplis depuis `presetData.config`
  Le banner s'affiche mais `typeDistribution`, `serviceSector`, `anchorVilleSlugs` etc. gardent leurs valeurs par défaut
- ❌ Pas de bouton "Retirer le preset" (reset form)
- ❌ Pas de pré-remplissage de `totalTargetCount`, `batchSize` depuis preset config
- **Impact** : La fonctionnalité principale du prefill (raison d'être des presets) ne fonctionne pas

### Score total : 28 + 22 + 5 = **55/120**
**Verdict : 46% — PARTIAL**
