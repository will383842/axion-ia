# V5-05 — P0-4 qualityImprovementAttempts affiché dans ReviewDetailV2
## Score : 22/30

### Vérification ReviewDetailV2.tsx
```tsx
function QualityIterationsBadge({ attempts }: { attempts: number }): React.ReactElement {
  if (attempts === 0) return <span className="...grey...">0/2</span>;
  if (attempts === 1) return <span className="...amber-100...amber-700...">1/2</span>;
  return <span className="...red-100...red-700...">{attempts}/2 — max atteint</span>;
}
```

- ✅ Badge gris si X=0 (surface-soft)
- ✅ Badge jaune/amber si X=1 (`amber-100` bg + `amber-700` text)
- ✅ Badge rouge si X≥2 (`red-100` bg + `red-700` text)
- ✅ Toujours visible (pas conditionnel sur > 0)
- ✅ Format "X/2" avec note "max atteint" quand 2+
- ✅ `review.job.qualityImprovementAttempts` passé depuis page.tsx ligne 36

### Interface mise à jour
- ✅ `qualityImprovementAttempts: number` dans `ReviewData.job`
- ✅ `review.job.qualityImprovementAttempts` extrait depuis `include: { job: true }` en Prisma

### GAP D2 P4 — Dénominateur hardcodé
- ⚠️ Le max est hardcodé à "2" (`/2`)
- D2 P4 (axionia_p4_decisions_canoniques) dit :
  - `guide_pilier` / `landing_ville` → max 3 itérations
  - autres types → max 2 itérations
- La valeur devrait être lue depuis `ContentGenConfig.key="quality_max_iterations_*"` ou basée sur contentType
- **Pénalité** : -8 pts (drift D2 P4)

### Score : 22/30 (-8 pts hardcode vs D2 P4)
