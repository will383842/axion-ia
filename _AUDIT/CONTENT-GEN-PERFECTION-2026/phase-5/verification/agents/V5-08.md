# V5-08 — P1 ArticleFeedback, Tableau croisé géo, Progress bars, Dashboard actif, Anomaly

## Score : 82/180

### ArticleFeedback (/40 → 22/40)

- ✅ schema.prisma : `model ArticleFeedback { id/articleId/userId/type/comment/createdAt }`
- ✅ FK vers `Article.id` (onDelete: Cascade)
- ✅ Migration incluse dans `20260521170000`
- ✅ API `POST /api/admin/content-gen/articles/[id]/feedback/route.ts`
- ✅ Auth check : `if (!session?.user?.email) → 401`
- ✅ Validation type : `"up" | "down"` uniquement
- ⚠️ UI dans ReviewDetailV2 : utilise `<a href=...>` (liens HTML) au lieu de `fetch()` ou `<form action>`
  → cliquer le bouton navigue au lieu de POSTer l'API
- ❌ ISOLATION VIOLATION : `src/app/api/admin/content-gen/articles/[id]/feedback/route.ts`
  Whitelist isolation-check : `/^src\/app\/api\/content-gen\//` — le segment `/admin/` n'est pas whitelisté
  Correction requise : déplacer vers `src/app/api/content-gen/admin/articles/[id]/feedback/route.ts`
- **Pénalité** : -18 pts (link vs fetch -10, isolation -8)

### Tableau croisé géo (/50 → 18/50)

- ✅ `getJobsVilleSectorDetail(limit=200)` dans `geo.ts` : groupBy [anchorVilleSlug, status]
- ✅ Page `/geo/coverage-table/` + composant `CoverageCrossTableV2`
- ✅ Tableau HTML avec colonnes : Ville, État, Count, Score moyen
- ❌ Colonne serviceSector absente (supprimée pour TypeScript constraint)
- ❌ Aucun filtre (pas d'inputs pour filtrer par ville ou état)
- ❌ Pas d'export CSV (spec + D-P5-4)
- ❌ Pas de pagination (limit 200 fixe, pas de contrôle UI)
- ❌ Pas de tri interactif (colonnes non cliquables)
- **Pénalité** : -32 pts

### Progress bars (/40 → 28/40)

- ✅ `CityCoverageV2.tsx` : progress bar 39/120 villes avec pourcentage (pre-existant conservé)
- ✅ `ContentGenDashboardV2.tsx` : progress bars par campagne active `generatedCount/totalTargetCount`
- ⚠️ Couleur progress bar statique `bg-[color:var(--color-admin-info)]` — pas de couleur dynamique
  Spec : <33% rouge, 33-66% orange, >66% vert
  CityCoverageV2 : 39/120 = 32.5% → devrait être rouge, mais reste bleu
- **Pénalité** : -12 pts (pas de color coding)

### Dashboard campagnes actives (/30 → 18/30)

- ✅ Section "Campagnes actives" avec cartes (pre-existant ContentGenDashboardV2, lignes 99-120)
- ✅ Nom + progress bar + publiés/générés + failed count
- ✅ `listCampaigns("running")` en Prisma
- ❌ Pas de compteur "articles ce jour" (spec)
- ❌ Pas d'ETA dynamique `(target - published) / velocity` (spec)
- **Pénalité** : -12 pts

### Anomaly detection batch (/20 → 0/20)

- ❌ `checkAnomalies()` ABSENT de `content-monitoring-worker.ts`
  git log confirme : fichier modifié uniquement dans commit `b8bb6844` avec 3 fonctions (checkQueueStuck, checkSoft404, checkIndexationStagnant). checkAnomalies jamais ajouté.
- ❌ Pas de badge alerte sidebar `alert_count`
- ❌ Le sprint P5 ne l'a PAS implémenté (marqué "pre-existant" dans verdict mais c'est FAUX)
- **Pénalité** : -20 pts

### Total V5-08 : 22 + 18 + 28 + 18 + 0 = **86/180**

**Verdict : 48% — PARTIAL**
