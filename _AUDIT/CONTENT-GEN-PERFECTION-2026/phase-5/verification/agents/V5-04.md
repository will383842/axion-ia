# V5-04 — P0-3 MAX_PUBLISH_PER_DAY Champ UI
## Score : 20/40

### Vérification BatchesV2.tsx
- ✅ `maxPublishPerDay: number` dans `BatchConfig` interface
- ✅ `saveMaxPublish(formData: FormData)` server action interne
- ✅ `updateMaxPublishPerDay(Number(...))` appelé
- ✅ Input `type="number" min="1" max="1000" step="10"`
- ✅ `defaultValue={cfg.maxPublishPerDay}` — valeur courante affichée

### Vérification policies.ts
- ✅ `getMaxPublishPerDay()` : `readContentGenConfig("MAX_PUBLISH_PER_DAY", 30)`
- ✅ `updateMaxPublishPerDay(value)` : `requireAdmin()` + validation range + `writeContentGenConfig`
- ✅ `writeContentGenConfig` → audit trail SOC2 dans `ContentGenAuditLog`

### GAP CRITIQUE — Worker ne lit pas depuis DB
```ts
// content-publish-worker.ts — getEffectivePublishCap()
const envCap = process.env.MAX_PUBLISH_PER_DAY; // SEULEMENT env var!
if (envCap !== undefined && envCap !== "") {
  return parseInt(envCap, 10);
}
// Rampe progressive (60/300/600 articles) — jamais ContentGenConfig
```
- ❌ Le worker `content-publish-worker.ts` ne lit JAMAIS `ContentGenConfig.key="MAX_PUBLISH_PER_DAY"`
- La valeur stockée par l'UI est inutilisée par le worker
- **Impact** : changer le cap en UI n'a AUCUN effet sur le comportement du worker
- **Pénalité** : -15 pts (fonctionnalité principale cassée)

### Validation
- ⚠️ Pas d'erreur inline en cas de valeur hors range (erreur thrown côté serveur mais non gérée en UI)
- **Pénalité** : -5 pts

### Score : 20/40 (-15 worker gap critique, -5 validation UI)
