# V5-02 — P0-1 Boutons Pause/Resume liste campagnes
## Score : 58/80

### Vérification code
- ✅ `import { Pause, Play, PlayCircle } from "lucide-react"` — ligne 4
- ✅ `async function pauseRow(id: string)` → `pauseCampaign(id)` — ligne 34
- ✅ `async function resumeRow(id: string)` → `resumeCampaign(id)` — ligne 39
- ✅ `async function launchRow(id: string)` → `launchCampaign(id)` — ligne 44
- ✅ `r.status === "running"` → `<Pause size={16} />` bouton
- ✅ `r.status === "paused"` → `<Play size={16} />` bouton
- ✅ `r.status === "draft"` → `<PlayCircle size={16} />` bouton
- ✅ `completed`/`failed` → null (aucun bouton)
- ✅ `title="Mettre en pause"` / `title="Reprendre"` / `title="Lancer la campagne"`
- ✅ `aria-label="Mettre en pause cette campagne"` etc.
- ✅ Classe `admin-button-ghost` (couleur neutre, pas terracotta) ✅
- ✅ Actions câblées avec `.bind(null, r.id)` — correct Next.js 16

### Cohérence avec spec
- ✅ Icon-only boutons (pas de texte)
- ✅ Server Actions existantes `pauseCampaign`/`resumeCampaign` câblées
- ✅ BullMQ purge au pause via `pauseCampaign()` (qui purge jobs en attente)

### Tests UI navigateur
- ⚠️ NON POSSIBLE — pas de dev server + DB en environnement audit
- Test pause → status DB paused : à valider manuellement
- Test resume → status running : à valider manuellement
- Comportement BullMQ : à valider manuellement

### Score : 58/80 (-22 pts tests UI non effectués)
