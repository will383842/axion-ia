# V5-01 — Décisions Will D-P5-1 à D-P5-6

## Score : 74/150

### D-P5-1 — 6 presets CampaignTemplate (/40) → 28/40

**IMPLÉMENTÉ PARTIELLEMENT**

- ✅ Modèle Prisma `CampaignTemplate` ajouté à `prisma/schema.prisma`
- ✅ Migration `20260521170000_add_campaign_template_and_feedback/migration.sql` créée
- ✅ Seed `prisma/seeds/content-gen/seed-campaign-templates.ts` — 6 slugs corrects :
  `pme-audits`, `interventions-weekly`, `tpe-burst`, `eti-pilier`, `cities-paris`, `rss-daily`
- ⚠️ Migration NON appliquée en prod (Will doit exécuter `prisma migrate deploy`)
- ⚠️ Seed NON exécuté en prod (Will doit exécuter `pnpm content-gen:seed-templates`)
- ⚠️ UI `/coverage/presets` fonctionne en mode fallback statique (pas depuis DB)
- Test SQL `SELECT slug FROM campaign_templates` : IMPOSSIBLE sans migration prod
- **Pénalité** : -12 pts (migration/seed non exécutés = DB vide)

### D-P5-2 — Seuil qualité 60/100 (/20) → 5/20

**NON IMPLÉMENTÉ (drift)**

- ❌ `QUALITY_LOOP_DEFAULTS.minScoreThreshold = 75` dans policies.ts (pas 60)
- ❌ Pas de `ContentGenConfig.key="quality_reject_threshold"` seedé à 60
- ❌ Pas de harmonisation llm-judge.ts (0-10) ↔ QualityLoopV2 (0-100)
- ⚠️ Seule la décision est documentée, non implémentée dans le code
- `QualityIterationsBadge` hardcode "2" max sans lire depuis DB
- **Pénalité** : -15 pts (drift configuration D-P5-2)

### D-P5-3 — Reporting email lundi 8h (/30) → 0/30

**NON IMPLÉMENTÉ — EXPLICITEMENT SKIPPÉ**

- ❌ Pas de `weekly-quality-report-worker.ts`
- ❌ Pas de script `content-gen:trigger-weekly-report`
- SMTP non configuré localement (justification valide mais P2 backlog)
- Destinataire `williamsjullin@gmail.com` jamais wired

### D-P5-4 — Tableau croisé (pas heatmap) (/30) → 16/30

**IMPLÉMENTÉ PARTIELLEMENT**

- ✅ Page `/content-gen/geo/coverage-table` créée
- ✅ Tableau HTML `<table>` — PAS de heatmap SVG (D-P5-4 respecté)
- ✅ `getJobsVilleSectorDetail(limit=200)` dans `geo.ts`
- ❌ Pas de filtres (ville/état) dans l'UI
- ❌ Pas d'export CSV
- ❌ Pas de pagination (limit fixe 200 lignes)
- ❌ Colonne `serviceSector` absente (TypeScript constraint)
- **Pénalité** : -14 pts

### D-P5-5 — MAX_PUBLISH_PER_DAY UI (/15) → 12/15

**IMPLÉMENTÉ — GAP WORKER**

- ✅ Input numérique dans `BatchesV2` (range 1-1000, step 10)
- ✅ Server action `updateMaxPublishPerDay()` stocke dans `ContentGenConfig.key="MAX_PUBLISH_PER_DAY"`
- ✅ Audit trail via `writeContentGenConfig` → SOC2 `ContentGenAuditLog`
- ⚠️ Worker `content-publish-worker.ts` lit UNIQUEMENT `process.env.MAX_PUBLISH_PER_DAY` + rampe (pas ContentGenConfig DB) — la valeur UI n'est donc pas lue par le worker
- **Pénalité** : -3 pts

### D-P5-6 — Ordre sprint A puis B (/15) → 15/15

**✅ RESPECTÉ**

- Phase A commit `3e5bdbb1` (quick wins UX) AVANT Phase B commit `56f7b782`
- `git log --oneline` confirme l'ordre chronologique

### Total V5-01 : 28 + 5 + 0 + 16 + 12 + 15 = **74/150**

**Verdict : 49% — PARTIAL**
