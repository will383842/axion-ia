# V5-03 — P0-2 CTA "Nouvelle campagne" terracotta persistant
## Score : 44/60

### Vérification layout.tsx
```tsx
// src/app/[locale]/(admin)/[adminPrefix]/content-gen/layout.tsx
<Link href={`${base}/coverage/new`} className="admin-button-cta text-[length:var(--text-admin-xs)]">
  + Nouvelle campagne
</Link>
```

- ✅ `admin-button-cta` = `background-color: var(--color-terracotta)` = `#c24a1b` (globals.css:582)
- ✅ Layout wrapper Next.js 16 App Router — couvre TOUTES les sous-routes `/content-gen/*`
- ✅ `sticky top-0 z-20` — CTA stick en haut au scroll
- ✅ Lien vers `/content-gen/coverage/new`
- ✅ Fil d'ariane `Content Generator` avec lien retour dashboard
- ✅ Border-b, bg-surface — intégré au design system admin

### Couverture des sous-pages
- ✅ Couvert : /coverage, /quality, /geo, /costs, /jobs, /settings/*, /rss, /templates, /kb-readonly, /review-queue, /city-coverage, /similarity-monitor, /orchestrator
- ⚠️ DOUBLON : `/content-gen` dashboard a DÉJÀ son propre CTA `admin-button-cta` dans AdminPageHeader. 2 CTAs visibles sur le dashboard.

### Vérification couleur
- ✅ `admin-button-cta` → `var(--color-terracotta)` → `#c24a1b` ✅ (jamais bleu)
- ✅ Hover : `var(--color-terracotta-deep)` → `#8c3010` ✅

### Tests UI navigateur
- ⚠️ NON POSSIBLE — pas de dev server en cours
- Screenshot terracotta : à valider manuellement

### Score : 44/60 (-8 doublon dashboard, -8 test UI impossible)
