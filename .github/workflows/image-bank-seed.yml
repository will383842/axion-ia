name: Image Bank · Seed + Enrich (SEO/AEO/GEO)

# Pipeline image-bank post-deploy :
#   1. Disk prune Hetzner (containers/images stoppés — safe)
#   2. Seed DB 133 ImageAssets via tsx (TypeScript)
#   3. Enrichissement via enrich-images.cjs (Node.js pur, fetch natif, 0 npm install)
#
# Déclenchements :
#   - Automatique : après chaque **job `deploy`** réussi de deploy-coolify.yml
#     (et NON après le workflow entier — cf. le job `porte` : un gate
#     Lighthouse rouge ou une compilation annulée éteignaient tout le seed)
#   - Manuel : GitHub Actions → Run workflow
#
# Idempotent : seed upsert par slug, enrich skip images avec metaTitle existant.

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (affiche plan, pas d'écriture DB)"
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]
      skip_enrich:
        description: "Skip enrichissement (seed seul)"
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]
      force_enrich:
        description: "Force re-enrichissement de toutes les images"
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]

  workflow_run:
    workflows: ["Build & Deploy · GHCR + Coolify (axion-ia.com)"]
    types: [completed]
    branches: [main]

permissions:
  contents: read
  # Requis par le job `porte` : lire la conclusion du job `deploy` de l'exécution
  # amont via l'API. Sans cette permission, `gh api …/jobs` renvoie 403 et la
  # porte resterait fermée — c'est-à-dire le défaut qu'elle répare.
  actions: read

jobs:
  # ── Porte de déclenchement — GEO-089 (audit GEO/AEO 2026-08-14, lot 18) ──────
  #
  # 🔴 CE JOB EXISTE PARCE QUE LE SEED NE PARTAIT PLUS. L'audit a relevé
  # « 24 exécutions, toutes en mai 2026 ». Cause racine tracée le 2026-08-16 :
  #
  # la condition était `github.event.workflow_run.conclusion == 'success'`,
  # c'est-à-dire la conclusion du **workflow de déploiement ENTIER**. Or ce
  # workflow ne fait pas que déployer : il enchaîne un gate Lighthouse, un ping
  # IndexNow, un sweep de chauffe. Et son job `build` porte
  # `cancel-in-progress: true`.
  #
  # Conséquences, toutes les deux observées :
  #   - une compilation annulée par la suivante → conclusion `cancelled`
  #     → seed sauté, alors qu'aucun déploiement n'a même eu lieu ;
  #   - un gate Lighthouse rouge → conclusion `failure`
  #     → seed sauté, alors que **le déploiement, lui, a réussi**.
  #
  # 🔑 Le seed dépend d'UNE chose : que le conteneur tourne la nouvelle image.
  # C'est le job `deploy` qui l'établit. Le coupler au verdict d'un gate de
  # qualité indépendant, c'est éteindre tout le pipeline image à la première
  # régression Web Vitals — sans que personne ne fasse le lien.
  #
  # On interroge donc la conclusion du JOB `deploy`, pas celle du workflow.
  porte:
    name: Le job deploy a-t-il reussi ?
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      lancer: ${{ steps.verdict.outputs.lancer }}
    steps:
      - name: Resoudre la conclusion du job `deploy`
        id: verdict
        env:
          GH_TOKEN: ${{ github.token }}
          EVENEMENT: ${{ github.event_name }}
          RUN_ID: ${{ github.event.workflow_run.id }}
        run: |
          set -euo pipefail
          if [ "$EVENEMENT" = "workflow_dispatch" ]; then
            echo "Declenchement manuel — on lance."
            echo "lancer=true" >> "$GITHUB_OUTPUT"; exit 0
          fi
          # `--jq` filtre cote serveur : on ne rapatrie pas tout le payload.
          # ⚠️ Le nom ci-dessous est le `name:` EXACT du job dans
          # deploy-coolify.yml — « Trigger Coolify deploy », pas « Deploy ».
          # Un filtre approximatif ne matcherait rien et refermerait la porte en
          # silence : exactement le defaut qu'on repare ici. La coherence des
          # deux fichiers est verrouillee par
          # tests/unit/ci/seed-images-declencheur.spec.ts.
          JOBS_URL="repos/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}/jobs?per_page=100"
          FILTRE='.jobs[] | select(.name == "Trigger Coolify deploy") | .conclusion'
          CONCLUSION=$(gh api "$JOBS_URL" --jq "$FILTRE" | head -1)
          echo "Conclusion du job deploy : '${CONCLUSION:-<introuvable>}'"
          if [ "$CONCLUSION" = "success" ]; then
            echo "lancer=true" >> "$GITHUB_OUTPUT"
          else
            # Pas un echec de CE workflow : il n'y avait simplement rien a semer.
            echo "Le deploiement n'a pas abouti — seed saute (ce n'est pas une erreur)."
            echo "lancer=false" >> "$GITHUB_OUTPUT"
          fi

  seed-and-enrich:
    name: Seed image bank + Enrich SEO/AEO/GEO
    runs-on: ubuntu-latest
    timeout-minutes: 55 # seed ~2min + tsx install ~2min + 133×17s enrich ~38min + buffer
    needs: porte
    if: needs.porte.outputs.lancer == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # ── SSH ──────────────────────────────────────────────────────────────────

      - name: Setup SSH
        env:
          HETZNER_SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
        run: |
          if [ -z "${HETZNER_SSH_KEY:-}" ]; then
            echo "::error::Secret HETZNER_SSH_KEY manquant."
            exit 1
          fi
          mkdir -p ~/.ssh
          echo "$HETZNER_SSH_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H -T 8 178.105.55.15 >> ~/.ssh/known_hosts 2>/dev/null || true
          ssh -o BatchMode=yes -o ConnectTimeout=10 root@178.105.55.15 'echo SSH_OK'

      - name: Wait for container restart after deploy
        if: github.event_name == 'workflow_run'
        run: |
          echo "⏳ Attente 3 min pour que Coolify redémarre le container…"
          sleep 180
          echo "✓ Prêt."

      # ── Disk cleanup ─────────────────────────────────────────────────────────

      - name: Libérer espace disque Hetzner
        run: |
          ssh -o BatchMode=yes root@178.105.55.15 bash <<'ENDSSH'
            echo "=== Disk avant ===" && df -h / | tail -1
            docker container prune -f 2>&1 | tail -2 || true
            docker image prune -f 2>&1 | tail -2 || true
            docker builder prune -f 2>&1 | tail -2 || true
            echo "=== Disk après ===" && df -h / | tail -1
          ENDSSH

      # ── Find container ────────────────────────────────────────────────────────

      - name: Find axion-ia container
        id: container
        env:
          COOLIFY_APP_UUID: ${{ secrets.COOLIFY_APP_UUID }}
        run: |
          set -euo pipefail
          ROWS=$(ssh -o BatchMode=yes root@178.105.55.15 \
            'docker ps --format "{{.Names}}|{{.Status}}"' 2>&1)
          NAME=$(echo "$ROWS" | awk -F'|' -v uuid="$COOLIFY_APP_UUID" \
            '$1 ~ "^"uuid {print $1; exit}')
          if [ -z "$NAME" ]; then
            echo "::error::Container $COOLIFY_APP_UUID introuvable."
            echo "$ROWS"; exit 1
          fi
          echo "✓ Container: $NAME"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"

      # ── Pre-check DB ──────────────────────────────────────────────────────────

      - name: Pre-check DB counts
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
        run: |
          echo "── Counts avant seed ──"
          ssh -o BatchMode=yes root@178.105.55.15 \
            "docker exec '$CONTAINER' node -e \
            \"try{const{PrismaClient}=require('/app/prisma/generated/client');
            const p=new PrismaClient();
            Promise.all([p.imageAsset.count(),
              p.imageAssetTranslation.count({where:{metaTitle:{not:null}}})
            ]).then(([a,e])=>{console.log('imageAsset:',a,'enrichies:',e);process.exit(0)})
            .catch(()=>process.exit(0));}
            catch(e){console.log('pre-check skip:',e.message.slice(0,60));process.exit(0);}\"" 2>&1 || true

      # ── [DRY RUN] ─────────────────────────────────────────────────────────────

      - name: "[DRY RUN] Afficher le plan"
        if: inputs.dry_run == 'true'
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
        run: |
          echo "::notice::DRY_RUN — 133 images à seeder + enrichissement FR"
          ssh -o BatchMode=yes root@178.105.55.15 \
            "docker exec '$CONTAINER' ls /app/prisma/seeds/image-bank/ 2>/dev/null" 2>&1 || true

      # ── Wait for DB ready ─────────────────────────────────────────────────────
      # Après un deploy, Postgres peut être en recovery pendant ~30-90s.
      # On poll jusqu'à ce que la DB réponde avant de seeder.

      - name: Attendre que la DB soit prête
        if: inputs.dry_run != 'true'
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
        run: |
          echo "⏳ Poll DB (max 3 min)…"
          for i in $(seq 1 18); do
            READY=$(ssh -o BatchMode=yes root@178.105.55.15 \
              "docker exec '$CONTAINER' node -e \
              \"const {PrismaClient}=require('/app/prisma/generated/client');
              const p=new PrismaClient();
              p.imageAsset.count()
                .then(()=>{console.log('READY');process.exit(0)})
                .catch(()=>process.exit(1))\"" 2>/dev/null || true)
            if [ "$READY" = "READY" ]; then
              echo "✓ DB prête (tentative $i)"
              break
            fi
            echo "  [$i/18] DB pas encore prête, attente 10s…"
            sleep 10
          done

      # ── Step 1 : Seed (node CJS pur — pas de tsx) ────────────────────────────
      # seed-images.cjs = CommonJS pur, require('/app/prisma/generated/client').
      # Zéro npm install, zéro tsx. Fiable sur disque plein.

      - name: Copier seed-images.cjs → container
        if: inputs.dry_run != 'true'
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
        run: |
          set -euo pipefail
          scp -o BatchMode=yes -o StrictHostKeyChecking=no \
            scripts/seed-images.cjs \
            root@178.105.55.15:/tmp/seed-images.cjs
          ssh -o BatchMode=yes root@178.105.55.15 \
            "docker cp /tmp/seed-images.cjs '$CONTAINER':/tmp/seed-images.cjs"
          echo "✓ seed-images.cjs copié"

      - name: Exécuter seed (133 ImageAssets)
        if: inputs.dry_run != 'true'
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
        run: |
          set -euo pipefail
          echo "── Seed 133 ImageAssets (upsert idempotent, node pur) ──"
          ssh -o BatchMode=yes root@178.105.55.15 \
            "docker exec '$CONTAINER' sh -c \
            'node /tmp/seed-images.cjs 2>&1'" 2>&1
          echo "── Seed terminé ──"

      # ── Step 2 : Enrich (node CJS pur — pas de tsx) ───────────────────────────
      # enrich-images.cjs = CommonJS pur :
      #   - fetch() natif Node.js 22 (pas @anthropic-ai/sdk)
      #   - require('/app/prisma/generated/client') (pas d'import ESM)
      # Zéro npm install → disque pas touché → fiable à chaque run.

      - name: Copier enrich-images.cjs → container
        if: inputs.dry_run != 'true' && inputs.skip_enrich != 'true'
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
        run: |
          set -euo pipefail
          scp -o BatchMode=yes -o StrictHostKeyChecking=no \
            scripts/enrich-images.cjs \
            root@178.105.55.15:/tmp/enrich-images.cjs
          ssh -o BatchMode=yes root@178.105.55.15 \
            "docker cp /tmp/enrich-images.cjs '$CONTAINER':/tmp/enrich-images.cjs"
          echo "✓ enrich-images.cjs copié dans /tmp du container"

      - name: Enrichissement SEO/AEO/GEO via Claude Vision (FR)
        if: inputs.dry_run != 'true' && inputs.skip_enrich != 'true'
        timeout-minutes: 45
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
          FORCE_FLAG: ${{ inputs.force_enrich == 'true' && '--force' || '' }}
        run: |
          set -euo pipefail
          echo "── Enrichissement Claude Vision (micro-batches de 10 images) ──"
          # Micro-batches : chaque run node est indépendant (mémoire réinitialisée).
          # Le script est idempotent : skip les images déjà enrichies.
          # --max-old-space-size=96 → GC agressif, compatible avec container slim.
          BATCH=0
          while true; do
            BATCH=$((BATCH+1))
            echo "  [Batch $BATCH] Enrichissement..."
            OUTPUT=$(ssh -o BatchMode=yes root@178.105.55.15 \
              "docker exec -e ENRICH_DELAY_MS=1200 '$CONTAINER' sh -c \
              'cd /app && node --max-old-space-size=96 /tmp/enrich-images.cjs \
               $FORCE_FLAG --limit 10 2>&1'" 2>&1)
            echo "$OUTPUT"
            # Si script indique plus rien à enrichir → terminé
            # Note: "déjà enrichies" = 0 restantes, "📸  0 " = 0 images (avec espace)
            if echo "$OUTPUT" | grep -q "déjà enrichies"; then
              echo "✅ Toutes les images enrichies."
              break
            fi
            if echo "$OUTPUT" | grep -qF "📸  0 image"; then
              echo "✅ Plus d'images à enrichir."
              break
            fi
            # Pause entre batches (laisse le container GC)
            sleep 5
          done
          echo "── Enrichissement terminé ──"

      # ── Post-check ────────────────────────────────────────────────────────────

      - name: Post-check DB counts
        if: always()
        env:
          CONTAINER: ${{ steps.container.outputs.name }}
        run: |
          echo "── Counts après seed + enrich ──"
          ssh -o BatchMode=yes root@178.105.55.15 \
            "docker exec '$CONTAINER' node -e \
            \"try{const{PrismaClient}=require('/app/prisma/generated/client');
            const p=new PrismaClient();
            Promise.all([p.imageAsset.count(),
              p.imageAssetTranslation.count({where:{languageCode:'fr'}}),
              p.imageAssetTranslation.count({where:{languageCode:'fr',metaTitle:{not:null}}})
            ]).then(([a,t,e])=>{
              console.log('imageAsset:',a,'| trFr:',t,'| enrichies:',e);
              process.exit(0);
            }).catch(()=>process.exit(0));}
            catch(e){console.log('post-check skip:',e.message.slice(0,60));process.exit(0);}\"" 2>&1 || true
